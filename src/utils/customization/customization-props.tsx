import { ISizingWrapperProps } from "../../components/sizing-wrapper/sizing-wrapper";
import { IVariantProps } from "../variant/variant-props";

export interface ICustomizationProps {
    sizing?: ISizingWrapperProps,
    variants?: IVariantProps
}
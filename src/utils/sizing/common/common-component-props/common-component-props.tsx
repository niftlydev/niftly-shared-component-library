import { PaddingSize } from '../../calculate-padding/calculate-padding';
import { ComponentSize } from '../../calculate-component-size/calculate-component-size';

export interface ICommonComponentProps {
    size?: ComponentSize;
    padding?: PaddingSize; 
}

